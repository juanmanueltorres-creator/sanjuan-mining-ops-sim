export function formatCoordinates(_lat: number, _lon: number): string {
  return '';
}

export function formatElevation(_elevationM: number | null | undefined): string {
  return '';
}

export function selectScaleBarMeters(_maxDistanceM: number): number | null {
  return null;
}
